const calc = {
    soma: (a: number, b: number): number => a + b,
    subtracao: (a: number, b: number): number => a - b,
    multiplicacao: (a: number, b: number): number => a * b,
    divisao: (a: number, b: number): number => a / b
};

export default calc;